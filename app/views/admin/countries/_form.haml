= form_for [:admin, @country] do |f|

  = render partial: 'admin/shared/errors', locals: { model: @country }

  .row
    .col-xs-4
      .form-group
        = f.label :name
        = f.text_field :name, class: 'form-control'

  .form-group
    = f.submit t('actions.save'), class: 'btn btn-primary'