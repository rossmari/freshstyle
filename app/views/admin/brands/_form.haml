= form_for [:admin, @brand]  do |f|

  = render partial: 'partials/errors', locals: { model: @brand }

  .form-group
    = f.label :title
    = f.text_field :title, class: 'form-control'

  .form-group
    = f.submit t('actions.save'), class: 'btn btn-primary'