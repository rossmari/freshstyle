.row
  filters

.row
  .col-md-offset-3.col-md-6.text-center
    .h3
      Категория товара


-# @goodes.each do |good|
- (1..40).to_a.in_groups_of(4).each do |group|
  .row
    - group.each do |item|
      = render partial: 'good_cell'



